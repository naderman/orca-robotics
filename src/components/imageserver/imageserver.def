DefaultComponentTag=ImageServer

# Provided Interfaces
Provides.Image.Type=::orca::Image
Provides.Image.DefaultName=image

# Required Interfaces

# Config Parameters
Config.DriverLib.Comment=Valid values are at least: { 'libHydroImageFake.so', 'libHydroImageFile.so', }
Config.DriverLib.Default=libHydroImageFake.so

Config.ImageWidth.Default=640
Config.ImageHeight.Default=480
Config.Format.Default=ImageBgr

# driver-specific configs
Config.Fake.Mode.Comment=Valid values are {'noise','smooth'}
Config.Fake.Mode.Default=noise
