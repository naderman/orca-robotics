DefaultComponentTag=LogPlayer

# Provided Interfaces
# No entries here, logplayer reads provided interfaces from masterfile

# Required Interfaces

# Config Parameters
Config.FactoryLibNames.Default=libOrcaLogFactory.so
Config.ReplayController.Default=interactive
Config.LogFilename.Default=master.log
Config.AutoStart.Default=0
Config.ReplayRate.Default=1.0
Config.BeginTime.Comment=Time format: DD:HH:MM:SS.uuuuuu
Config.BeginTime.Default=0:0:0:0.0
Config.RequireAll.Default=1

# Interface-specific config
